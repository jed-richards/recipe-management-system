services:
  db:
    image: postgres:16
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: rms
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
